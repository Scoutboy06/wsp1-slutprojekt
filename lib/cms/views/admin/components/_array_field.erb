<div class="flex flex-col gap-2">
  <p x-text="field.name"></p>
  <div class="px-4 py-4 border border-gray-400 rounded">
    <template x-for="(item, index) in arrayValues[field.name]" :key="index">
      <p x-text="JSON.stringify(item)"></p>
    </template>
  </div>
</div>